syntax = "proto3";

import "position/proto/position.proto";

package pack.render.proto;

message Camera {
  string name = 1;
  pack.position.proto.Position position = 2;
  pack.position.proto.Orientation orientation = 3;
}
