syntax = "proto3";

import "position/proto/position.proto";

package pack.render.proto;

message Camera {
  pack.position.proto.Position position = 1;
  pack.position.proto.Orientation orientation = 2;
}
