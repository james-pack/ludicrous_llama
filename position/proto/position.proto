syntax = "proto3";

package pack.position.proto;

message Orientation {
  // TODO(james): Original form from GLFW example code. Consider restructuring as quaternions.
  float rot_x = 1;
  float rot_y = 2;
  float rot_z = 3;
}

message Position {
  float x = 1;
  float y = 2;
  float z = 3;
}
